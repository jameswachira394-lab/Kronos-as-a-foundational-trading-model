numpy
pandas
pyyaml
matplotlib

# Optional, only needed for live/paper MT5 execution (Windows or Wine):
# MetaTrader5

# Kronos itself is not on PyPI — clone it and put it on your path:
#   git clone https://github.com/shiyu-coder/Kronos.git vendor/Kronos
#   pip install -r vendor/Kronos/requirements.txt
# (torch, einops, huggingface_hub, safetensors, tqdm, etc.)
