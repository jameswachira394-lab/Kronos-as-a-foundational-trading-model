# ============================================================
# Kronos Trading System — Python dependencies
# ============================================================

# ------------------------------------------------------------
# CORE  (always needed — run_demo.py, backtest --mock, etc.)
# ------------------------------------------------------------
numpy
pandas
pyyaml
matplotlib
scipy          # financial metrics (may be used by metrics extensions)

# ------------------------------------------------------------
# KRONOS AI  (needed for real forecasting with NeoQuasar/Kronos)
# Kronos is NOT on PyPI. Clone it first:
#   git clone https://github.com/shiyu-coder/Kronos.git vendor/Kronos
# Then install its requirements:
#   pip install -r vendor/Kronos/requirements.txt
# Which typically pulls in:
# ------------------------------------------------------------
# torch>=2.0
# einops
# huggingface_hub
# safetensors
# tqdm
# transformers

# ------------------------------------------------------------
# MT5 EXECUTION  (live/paper trading via MetaTrader 5)
# Windows only (or Wine on Linux/macOS).
# Uncomment when you are ready for live/paper execution:
# ------------------------------------------------------------
# MetaTrader5
